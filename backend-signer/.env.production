# Production Environment Configuration
NODE_ENV=production
LOG_LEVEL=info

# Network Configuration
SEI_RPC_URL=https://evm-rpc.sei-apis.com
SEI_CHAIN_ID=1329

# Contract Addresses (Mainnet)
# These addresses need to be updated with actual mainnet deployments
AI_ORACLE_ADDRESS=
VAULT_FACTORY_ADDRESS=

# Vaults to Monitor (Production vault addresses)
# These should be populated with actual production vault addresses
VAULTS=

# AI Engine Configuration
# Updated to use ML-powered AI engine with RL agent, LSTM forecaster, and IL predictor
AI_ENGINE_URL=https://yield-delta-ai-engine.up.railway.app

# Service Configuration
CRON_SCHEDULE=*/5 * * * *  # Every 5 minutes for production
MIN_CONFIDENCE_THRESHOLD=8000  # Higher threshold for production
REQUEST_DEADLINE_SECONDS=300
AI_MODEL_VERSION=liquidity-optimizer-v1.0

# Signer Configuration
# CRITICAL: Must be set via environment variable or secret manager
# NEVER commit the actual private key to the repository
AI_MODEL_SIGNER_PRIVATE_KEY=