# Cloudflare Pages specific environment variables
# These are used to enable Cloudflare-specific build optimizations

# Disable features that cause issues in Cloudflare Pages
NEXT_PUBLIC_CLOUDFLARE_BUILD=true

# Use more conservative build settings
NODE_ENV=production